using System;

namespace Pokemons.API.Data.CsvStream.CsvData
{
    // Only exists so othet Data objects can share the same base class
    // So abstraction is possible for CsvContext classes
    public class CsvDataObject
    {
        
    }
}
